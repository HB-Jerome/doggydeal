{% extends 'base.html.twig' %}

{% block title %}Hello AdoptantController!{% endblock %}

{% block body %}
<div class="container">
        {{ form_start(formPassword) }}
		{{ form_widget(formPassword) }}
		

		<button type="submit">Verifier</button> 
	    {{ form_end(formPassword)}} 

        {{ form_start(form) }}
		{{ form_widget(form) }}
		

		<button type="submit">Verifier</button> 
	    {{ form_end(form)}} 
        
	</div>
{% endblock %}
